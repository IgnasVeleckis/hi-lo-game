/* class Message {
    constructor(text, condition) {
        this.text = text;
        this.condition = condition; // win loss tie insufficient funds ir tt ir per if tikrint if(message1.condition === win){atitinkamas text ir color}
    }
} */